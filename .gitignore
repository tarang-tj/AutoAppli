node_modules/
.next/
.env
.env.local
.env.*.local
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/
.DS_Store
.claude/
.claude.backup-existing/

# Local scratch migration/fix scripts kept at root by the dev.
# Not intended for the repo — they're one-shot ops tooling.
/apply-*.sh
/fix.sh
/fix-*.sh

# Accidental duplicate files from macOS Finder ("file 2.ext")
**/* 2.*
**/* 3.*

# Supabase CLI local state (auth, project metadata)
supabase/.temp/
