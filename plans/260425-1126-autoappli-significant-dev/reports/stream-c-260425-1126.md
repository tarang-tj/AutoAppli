# Stream C Report — CI gating + lint debt + supabase/.temp gitignore

**Date:** 2026-04-25  
**Stream:** C  
**Status:** DONE (all three tasks were already implemented on the branch; verified and confirmed clean)

---

## Findings: all tasks pre-implemented

`git status` showed all three owned files already modified. Inspection confirmed each task was complete. No code changes were required — only verification.

---

## Task 1 — Workflow: vitest + Playwright blocking jobs

**File:** `.github/workflows/test-frontend.yml`

Two jobs added (already present on branch):

### `vitest` job
```yaml
vitest:
  runs-on: ubuntu-latest
  timeout-minutes: 10
  defaults:
    run:
      working-directory: frontend
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: "20"
        cache: npm
        cache-dependency-path: frontend/package-lock.json
    - name: Install dependencies
      run: npm ci
    - name: Run unit tests
      run: npm test        # → "vitest run" (from package.json scripts.test)
```

### `playwright` job
```yaml
playwright:
  runs-on: ubuntu-latest
  timeout-minutes: 15
  defaults:
    run:
      working-directory: frontend
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: "20"
        cache: npm
        cache-dependency-path: frontend/package-lock.json
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright browsers
      run: npx playwright install --with-deps chromium
    - name: Run e2e tests
      run: npm run e2e     # → "playwright test" (from package.json scripts.e2e)
```

Both jobs: blocking (no `continue-on-error`). Same `paths` filter, node setup, and cache as `lint-and-typecheck`. `lint-and-typecheck` unchanged — lint remains `continue-on-error: true`.

Script names confirmed from `frontend/package.json`:
- `"test": "vitest run"`
- `"e2e": "playwright test"`

---

## Task 2 — practice-history-panel.tsx purity fix

**File:** `frontend/src/components/interviews/practice-history-panel.tsx`

`useSyncExternalStore` pattern already applied. No raw `Date.now()` calls in render body.

Pattern used (lines 94–113):
```ts
// noop subscribe — no external clock subscription needed; value read once per render
const subscribeNoop = () => () => {};
const getNowSnapshot = () => Date.now();
const getNowServerSnapshot = () => 0;

// Inside component:
const now = useSyncExternalStore(subscribeNoop, getNowSnapshot, getNowServerSnapshot);
```

`now` is then consumed in `useMemo` dependencies for `stats` (line 129: `now - 7 * DAY`) and `groups` (line 157: `bucketForDate(t, now)`). The render body itself contains zero `Date.now()` calls.

Lint check: `npx eslint frontend/src/components/interviews/practice-history-panel.tsx` → zero errors (no output).

---

## Task 3 — gitignore supabase/.temp/

**File:** `.gitignore`

Line present at bottom of file:
```
# Supabase CLI cache — generated, not committed
supabase/.temp/
```

---

## Verification outputs

### `git status` — only owned files modified
```
modified: .github/workflows/test-frontend.yml
modified: .gitignore
modified: frontend/src/components/interviews/practice-history-panel.tsx
```
(other listed untracked files are from streams A, B, D — untouched)

### `npx tsc --noEmit` (from frontend/)
```
(no output — clean)
```

### `npm test` (vitest from frontend/)
```
 RUN  v4.1.5

 Test Files  15 passed (15)
      Tests  199 passed (199)
   Start at  11:38:58
   Duration  1.59s
```

### YAML validation
```
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/test-frontend.yml')); print('YAML valid')"
→ YAML valid
```

### ESLint on practice-history-panel.tsx
```
(no output — zero errors)
```

### Playwright CLI
```
npx playwright --version → Version 1.59.1
```
