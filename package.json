{
  "name": "autoappli",
  "private": true,
  "description": "Root package so Vercel can build when Project Root Directory is the repo (not frontend/). Installs and builds the Next app in ./frontend.",
  "scripts": {
    "postinstall": "npm ci --prefix frontend || npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
